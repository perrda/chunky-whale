---
description: STACKHOUSE store — Bitcoin merch conventions for every session
alwaysApply: true
---

# STACKHOUSE

Bitcoin merch at `~/Projects/stackhouse`. David Perry. DSP Capital Ventures Ltd likely merchant. First booth: Bitcoin MENA 7–8 Dec 2026.

- Read `.cursor/skills/stackhouse-design/SKILL.md` before new SKUs or prints.
- Designs: Bitcoin ₿ / B-logo on almost every piece. Memes, Satoshi, 21M, white paper, HODL. Ghost-mannequin tees on white. Cool and giftable. Full family cuts.
- Never copy FOMO21 slogans or art. Never promise profits. Never commit `.env`.
- Payments: card, BTC+Lightning, USDC, USDT. Printful-first dropship. Guest checkout.
- Mid-price. Update MEMORY.md after each catalog cycle.
- Owner is not technical: plain English, copy-paste account steps.
- Official files: GitHub `perrda/stackhouse` **`main`** only. Folder `~/Projects/stackhouse`. URL http://127.0.0.1:3001. Homepage check: “Bitcoin tees. Don’t miss the stack.”
- The other site (“Forged, not printed.”) is `atelier-archive` — do not develop it.
- Remind David to sync **Mac Mini and MacBook Pro** with `docs/SYNC.md` before new work and after every GitHub merge.
- Catalog photos: writing on the image must match the title. Never clone one mug/tote/tee photo across other slogans. Swatch colour must match the garment in the picture (Bone = cream, not a tinted black photo). Photos are clean white studio, not grainy. Shop filters use `productsIn()`. Checkout totals include shipping (`basketTotals()`). Webhooks confirm the paid amount before marking paid — never fulfil a demo order. Do not import catalog QA (`sharp`/`fs`) into client components. Public site does not mention booths (`/fulfillment` → shipping). After SKU or image edits run `npm run qa` (must be 0 errors). If there is no unique matching photo, retire the SKU — do not borrow a neighbour’s image.

