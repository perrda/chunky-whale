# STACKHOUSE — notes

**Official site (19 Aug 2026):** GitHub `perrda/stackhouse` branch **`main`**.  
Homepage check: **“Bitcoin tees. Don’t miss the stack.”** + orange ₿ badge + STACK**HOUSE**.  
Version **0.6.0**. Strapline: **Stack sats. Wear ₿.** Guest checkout. Login optional.

If you see **“Forged, not printed.”** you are on the **wrong** shop. That copy is frozen as **`atelier-archive`**. Do not develop it. Do not merge it into `main`.

## How we got here (do not repeat the mix-up)

- David’s real shop lived only on the **MacBook** (`backup-before-update` → commits `22524d6`, `e994494`).
- GitHub `main` had briefly been a different atelier site. That was saved as `atelier-archive`, then `main` was pointed at the MacBook shop.
- Safety copies still on GitHub: `correct-site` (same as official `main`) and `atelier-archive` (the other site).

## Devices (keep them the same)

- **MacBook Pro** (`DSP-MacBook`) and **Mac Mini** both use `~/Projects/orangeforge` until the folder is renamed to `~/Projects/stackhouse`.
- Always `git pull` / `git fetch` + `git reset --hard origin/main` so both Macs match GitHub `main`.
- iPhone / iPad: same public site later; check portrait **and** landscape. Local preview: http://127.0.0.1:3001 (Mac only).

## Brand and repo

- Visible name: **STACKHOUSE**. Merchant likely DSP Capital Ventures Ltd.
- Only GitHub repo going forward: **`perrda/stackhouse`**. The old `orangeforge` GitHub name already redirects here. Do not create a second repo. Delete leftover OrangeForge names only after both Macs are on `main` and David says it is safe.
- Folder/package still say `orangeforge` in some files — rename is a later pass. Do not commit `.env`.

## House rules

- Mega-nav: Trending, T-Shirts, Sweatshirts, Women, Hats, Kids, Drinkware, Blog, Collections. Colour swatches change photos.
- Vendor pack: `docs/VENDOR-ONBOARDING.md` + `docs/ARTWORK-SPEC.md`
- Bitcoin merch. Dry / insider. Full family cuts. Printful first.
- Never copy FOMO21. Never promise profits.
- First booth: Bitcoin MENA, Abu Dhabi, 7–8 Dec 2026.
- Owner is not technical: plain English, copy-paste steps.

Skill (current filename): `.cursor/skills/orangeforge-design/` · Rule: `.cursor/rules/orangeforge.mdc`  
Preview: http://127.0.0.1:3001
